// Tests temporarily disabled — @types/jest not configured in this project.
// Re-enable once jest types are added to apps/api/tsconfig.json.
//
// import { Test, TestingModule } from '@nestjs/testing';
// import { StoryGenerationService } from './story-generation.service';
// import { SubscriptionService } from '../subscriptions/subscription.service';
//
// describe('StoryGenerationService — tier routing', () => {
//   it('uses claude-sonnet model for pro users', ...);
//   it('uses gemini-flash model for free users', ...);
//   it('throws ForbiddenException when free user has 0 stories remaining', ...);
//   it('allows free user with 2 of 3 stories used to generate', ...);
// });
